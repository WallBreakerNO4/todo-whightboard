services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: todo-whiteboard-app
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
    volumes:
      - ./tasks.json:/app/data/tasks.json